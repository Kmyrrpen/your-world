import styled from "styled-components";

const DashboardList = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr;
  min-height: 100vh;
  gap: 1.5rem;
`;

const S = { DashboardList };
export default S;
